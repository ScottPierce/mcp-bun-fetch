{
  "name": "mcp-bun-fetch",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "bin": {
    "mcp-bun-fetch": "./dist/index.js"
  },
  "description": "MCP server for fetching web content",
  "scripts": {
    "build": "bun build index.ts --minify --sourcemap=linked --outdir=dist && echo '#!/usr/bin/env bun' | cat - dist/index.js > dist/tmp && mv dist/tmp dist/index.js",
    "style:fix": "biome check --write",
    "style:check": "biome check",
    "test": "bun test"
  },
  "dependencies": {
    "@types/turndown": "^5.0.6",
    "turndown": "^7.2.2",
    "zod": "^4.2.1"
  }
}
